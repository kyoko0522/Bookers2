<div class="container">
 <div class="row no-gutters">
<table class="table">
<main>
 <div class ="container px-5 px-sm-0">
  <div class="row">
   <div class="col-md-3">
<h2>User info</h2>
 <p><%= attachment_image_tag user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: "no_image.jpg" %>
   </p>
   <tbody>
    <tr>
     <th>name</th>
     <th><%= user.name %></th>
     </tr>
     <tr>
  <th>introduction:</th>
  <th><%= user.introduction %></th>
  </tr>
  </tbody>
  </table>
  <div class="row">
 <div class="col-md-3"><%= link_to "edit", edit_user_path(user) %></div>
  </div>


<div class="container">
 <div class="row no-gutters">
  <div class="col-md-3">
<h1>New book</h1>
<div class="form-body p-4">
<% if book.errors.any? %>
<%= book.errors.count %>errors prohibited this obj from being saved:
<ul>
    <% book.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
    </ul>
    <% end %>
</ul>
<%= form_with model:book, local:true do |f| %>

<h4>Title</h4>
<%= f.text_field :title %>

<h4>Opinion</h4>
<%= f.text_area :body %>

<%= f.submit'Create Book' %>

<% end %>
</div>
</div>
</div>
</div>




</div>
</div>
</div>
</div>
</div>
</main>